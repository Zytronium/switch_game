[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "switch_net"
version = "0.1.0"
description = "Raw non-IP/TCP Ethernet frame transport for the Switch Game"
requires-python = ">=3.8"

[tool.maturin]
module-name = "switch_net"
